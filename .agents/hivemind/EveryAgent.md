# EveryAgent

Before any implementation, read the pinned canonical EveryAgent contract named
in `CONTRACT.json`. It defines skill routing, GitHub/Cloudflare MCP capability
discovery, model-tier limits, durable-state ownership, security-audit method,
and safe self-improvement. This consumer intentionally does not duplicate it.
